# Verflixed signing key

This keystore is the **same Android-debug cert** used for the 1.7.x Fire TV
builds (`SHA-256 981a04d3…`). In-app updates only install if the new APK is
signed with this exact key.

Do not replace it. A new key forces every user to uninstall first.

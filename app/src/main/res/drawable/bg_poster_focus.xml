<?xml version="1.0" encoding="utf-8"?>
<!-- Drawn as foreground over the artwork: tinted focus ring, hairline edge at rest. -->
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_focused="true">
        <layer-list>
            <!-- Dark keyline OUTSIDE the ring so it reads over bright artwork. -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <corners android:radius="11dp" />
                    <stroke
                        android:width="1dp"
                        android:color="#99000000" />
                </shape>
            </item>
            <item android:inset="1dp">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <corners android:radius="10dp" />
                    <stroke
                        android:width="3dp"
                        android:color="@color/sv_focus_ring_solid" />
                </shape>
            </item>
            <!-- Soft inner halo so focus reads from across the room. -->
            <item android:inset="4dp">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/transparent" />
                    <corners android:radius="7dp" />
                    <stroke
                        android:width="1dp"
                        android:color="@color/sv_focus_ring_soft" />
                </shape>
            </item>
        </layer-list>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@android:color/transparent" />
            <corners android:radius="10dp" />
            <stroke
                android:width="1dp"
                android:color="@color/sv_card_stroke" />
        </shape>
    </item>
</selector>
